# Frontend

This folder contains a minimal static frontend for EchoSQL.

Files:
- index.html — main HTML file
- styles.css — basic styling
- app.js — front-end logic that posts SQL to `/api/run-sql`

Open `index.html` in a static server or configure your backend to serve the `app/frontend` folder.
